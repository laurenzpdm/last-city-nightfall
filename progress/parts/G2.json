{"ts": "2026-08-16T00:00:00Z", "part": "G2", "title": "The lenses stopped shouting: one arbiter for every word on the world, a frame budget that falls with the zoom, and nothing of [P19]'s on [P17]'s clock", "verified": {"tests/overlays/run_lens_density.tscn (NEW, standalone, real _draw() over a city rebuilt to BE the frame the critic counted)": "TESTS PASSED — 99 checks, 0 failures. 21 measured frames: 6 lenses + the always-on layer at zoom 0.50 / 0.60 / 0.70.", "chips per frame, arbiter ON, zoom 0.50 / 0.60 / 0.70": "1-9 / 1-11 / 1-9 against budgets of 10 / 11 / 12", "overlaps per frame (pairs where at least one is a word), all 21 frames": "0", "words standing on [P17]'s clock panel, all 21 frames": "0 (18 and 36 refused for it in the real run's freeze frames)", "worst repeat (most times one string appears), all 21 frames": "1-2", "SAME FRAMES with the arbiter bypassed (field.enforce = false), asserted by the suite as its own precondition": "42 chips, 80 overlaps, 11 words on the clock at zoom 0.60; 41 chips at 0.70 — the critic counted 41", "REVERT PROOF — the PRE-CHANGE production files, unmodified, in a scratch git worktree, watched by subclassing label()/plate()": "zoom 0.70: 70 chips, 35 overlaps, 20 words on the clock, worst repeat 22, 22 'building' chips, 3 'SURVIVAL LINE' labels on one contour. 9 of 9 frames would fail the new suite. The 22 and the 3 are the critic's own numbers, reproduced.", "same revert probe at zoom 0.50 / 0.60": "24 chips 1 overlap 3 on clock / 63 chips 26 overlaps 14 on clock", "tests/overlays/test_label_field.gd (NEW)": "13 cases over the five rules; total overlays part 75 tests, 754 asserts, TESTS PASSED", "real run artifacts/G2_vis3 (xvfb 1920x1080, first_night, 24k ticks) — per-lens density line in log.txt": "none 0/16 · heat_network 2/18 @1.20 · thermal 1/12 @0.70 · coverage 2/16 @1.00 · heat_network 4/11 @0.62 · freeze 4/10 @0.50 · bottleneck 1/12 @0.65 · freeze 4/14 @0.85 — every one 0 overlaps, 0 on chrome, worst repeat 1", "Log.error count in that run": "0", "artifacts/G2_vis3/shots/midday.world.png": "SURVIVAL LINE -10C once and COMFORT +10C once, each on its own contour (was three SURVIVAL labels on one closed loop)", "artifacts/G2_vis3/shots/deep_night.world.png": "3 temperature chips and one FROZEN plate, clock clean (was 24 temperature chips and a FROZEN -24C plate printed across the clock panel)", "artifacts/G2_vis2/shots/assault.world.png": "the ladder of 22 'building' chips is 3 merged markers carrying counts and one 'building x2' chip", "tests/boot/run_reachability.tscn --force-ui": "TESTS PASSED — 92 checks, 0 failures, 0 unchecked", "tests/hud/run_hud_view.tscn": "TESTS PASSED — 192 checks, 0 failures", "tests/run_tests.gd --part=hud": "40 tests, 172 asserts, TESTS PASSED", "tools/determinism.sh": "IDENTICAL sha256 5a7e260cd12f261d and db56216ca2ee60dd, DETERMINISM OK", "tools/parse_check.gd": "442 scripts, no parse errors"}, "open": ["PERF NOT MEASURED. Four other agents' render suites were on the box the whole session (frame_lab.tscn, night_contrast.tscn); every wall-clock number here would be noise. The added per-frame cost is one Rect2 loop over ~50 boxes per word plus [P17].chrome_rects() once per frame — INTEGRATOR must confirm against the real budget.", "tools/check.sh WAS NOT RUN END TO END (40+ min against a box running three other agents' visual suites). Ran by hand: parse, --part=overlays, --part=hud, run_hud_view.tscn, run_lens_density.tscn, run_reachability.tscn --force-ui, determinism.sh. tests/d7/run_layout_audit.tscn --force-ui had not printed a verdict when this was written; it needs a re-read on a quiet box.", "KNOWN LIMIT, visible in artifacts/G2_vis3/shots/build.png: GRID 3 is three tiles wide and every one of them sits inside the clock panel's rectangle, so its badge has nowhere honest to go and is not drawn. The lens offers 12 anchors per grid (north-to-south members, then the underside of the southern edge) and takes the first clear one; when a whole grid is behind a panel there is no clear one. The legend still names it with its hue and 'NO SOURCE', which is the fact that matters, and inventing a position for the badge would be a lie about where the grid is. If [P16] ever adds a leader line from chrome to world this is the case for it.", "[P17]/[P18]/[P22]: the keep-out list is built from the `lcn_hud_chrome` group's `chrome_rects()`. [P18]'s build menu (layer 74) and [P22]'s dilemma card (66) are NOT in that group, so a lens word can still be drawn under them. It reads better than the clock did because both are near-opaque, but if either wants to be keep-out it needs `chrome_rects()` and that group — LcnOverlayRoot._chrome_world() picks it up with no change on my side.", "[P19] follow-up not taken this wave: the merged status markers are capped to four badge-widths of reach (MARK_EXTENT), which at zoom 0.62 over a 230-building city still leaves 40 markers on screen. They are 22 px discs with glyphs, not words, and the critic counted chips — but if 'marks' is the next number a critic reads, the answer is a second merge pass keyed on problem rather than on distance."]}
